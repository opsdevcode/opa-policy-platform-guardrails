# guardrails

OPA guardrails policy pack for Terraform plans.

> **This repository was generated by [repave](https://repave.opsdevco.de).**
> Every file here — the Rego policies, Conftest fixtures, CI gates, and the
> `repave.yaml` provenance record pinning blueprint, standard, and engine version —
> was emitted in a single run from a short form, not written by hand.
>
> repave is the intelligent platform layer: describe the golden path once and it
> produces the repo your platform team would have hand-rolled, with policy enforced
> from the first commit.
>
> **[Join the waitlist](https://repave.opsdevco.de/waitlist)** to generate your own.

Generated by repave blueprint `opa-policy-generic`.

## Standard

This policy repo follows `standards/policy/opa.md` v1.2.0 and the
governance baseline in `standards/policy/governance-baseline.md`.

## Usage

Run Conftest against plan fixtures:

```bash
conftest test tests/fixtures -p policy
```

Add Rego under `policy/` and matching JSON (or YAML) inputs under `tests/fixtures/`.

## Provenance

- **Blueprint:** `opa-policy-generic` @ `0.3.0`
- **Standard:** `standards/policy/opa.md` @ `1.2.0`
- **Engine:** `2.34.0` (generated `2026-08-07T04:52:41+00:00`)
- **OPA policies:** `policy/opa/policies` @ `1.0.0`
- **Policy profile:** `opa-focused` (pack `repave-opa-pack`)
- **Canonical record:** `repave.yaml` (`provenance-drift` gate validates this file)

